import { expect } from 'chai'
import {Authenticator, setupGoogleApi} from "../src";
import {GoogleApi} from "dt-types";

describe('test case', () => {
    it('', async () => {
        // const auth = new Authenticator("../../data/credentials/token.json", "../../data/credentials/credentials.json")
        // const googleApi: GoogleApi = await setupGoogleApi(auth, '22f შესავალი ციფრულ ტექნოლოგიებში', "../../data/students.json")
        // const dueDate = await googleApi.classroom.getDueDate('დავალება 1')
        // console.log(dueDate);
    }).timeout(30000)
})

